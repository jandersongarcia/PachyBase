#!/usr/bin/env bash
set -euo pipefail

ROOT_DIR="$(cd "$(dirname "${BASH_SOURCE[0]}")" && pwd)"
CLI_SCRIPT="$ROOT_DIR/scripts/pachybase.sh"

if [[ ! -f "$CLI_SCRIPT" ]]; then
  echo "CLI script not found: $CLI_SCRIPT" >&2
  exit 1
fi

bash "$CLI_SCRIPT" "$@"
